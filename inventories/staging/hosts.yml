---
# NOTE : la VM Multipass n'existe pas encore à ce stade (tâche 1 = squelette seul).
# Remplacer ansible_host par l'IP réelle une fois la VM créée : `multipass info logichain-staging`.
logichain:
  hosts:
    logichain-staging:
      ansible_host: "203.0.113.10"
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519
