---
# Le runner GitHub Actions sert lui-même de machine vierge : le playbook
# complet y est joué en connexion locale, puis rejoué pour vérifier
# l'idempotence (voir .github/workflows/cd.yml).
logichain:
  hosts:
    localhost:
      ansible_connection: local
